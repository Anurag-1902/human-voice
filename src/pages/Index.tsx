// Kept for backwards-compatibility. The landing page now lives in Landing.tsx
// and the humanizer workspace in AppPage.tsx (see App.tsx routes).
import Landing from "./Landing";

const Index = () => <Landing />;

export default Index;
